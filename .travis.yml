language: c
install: wget https://raw.githubusercontent.com/ocaml/ocaml-travisci-skeleton/master/.travis-opam.sh
script:
- bash -ex .travis-opam.sh

sudo: required
env:
  - OCAML_VERSION=4.02 PACKAGE=tiger INSTALL=true TEST=true
  - OCAML_VERSION=4.01 PACKAGE=tiger INSTALL=true TEST=true
